/**
 * General Formatting/Cleaning
 */

// // Remove leftover HTML
// str.replace(/<\/?\w+((\s+\w+(\s*=\s*(?:".*?"|'.*?'|[\^'">\s]+))?)+\s*|\s*)\/?>/, '');
//
// // Remove HTML Encoded Entities
// str.replace(/(&)(?:[a-z,A-Z,0-9]+|#\d+|#x[0-9a-f]+);/, ' ');
//
// // Remove Symbols (Trademark)
// str.replace(/\u2122/, '');
//
// // Remove quantity notations
// str.replace(/^[0-9][^\.][xX\s\-\*]{1,3}|^[0-9][\s]\b|^\([0-9]\)[\s]\b|[xX\s]{2,3}[0-9]$/, '');
